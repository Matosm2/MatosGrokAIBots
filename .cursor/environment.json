{
  "name": "MatosGrokAIBots — trading-bot",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "trading-bot API (paper)",
      "command": "cd trading-bot && WEBHOOK_SECRET=dev-cloud-agent-secret-12345 TRADING_MODE=paper DATA_DIR=data ./.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI TradingView -> Binance Spot paper-trading webhook service on http://0.0.0.0:8000. Dev-only paper mode; auth header value is X-Webhook-Secret: dev-cloud-agent-secret-12345. Endpoints: /livez, /health, /trades, POST /webhook/tradingview, /dashboard."
    }
  ],
  "ports": [
    { "name": "trading-bot API", "port": 8000 }
  ]
}
