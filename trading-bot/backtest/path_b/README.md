# Path B research

TV-free offline bar-close backtests (not paper/live).

## fresh-wave-v1

Five new IDs under `fresh_wave/` — see that package README. Feed: `mtf_ohlcv/`.

```bash
PYTHONPATH=. python -m backtest.path_b.fresh_wave
```
